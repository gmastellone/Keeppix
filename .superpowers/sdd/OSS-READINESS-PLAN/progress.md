# SDD ledger — plan: OSS-READINESS-PLAN.md (relocated from docs/superpowers/plans/2026-08-27-keeppix-oss-readiness.md by Task 3)

Ruling: this SDD workspace lives at `.superpowers/sdd/2026-08-27-keeppix-oss-readiness/`, inside the exact directory tree Task 3 of this plan deletes (`.superpowers/sdd/`) — unlike the skill's assumption, this repo does NOT gitignore `.superpowers/`, it's tracked on purpose. Task 3's dispatch must explicitly exclude this one subdirectory from the deletion (delete every *other* phase ledger under `.superpowers/sdd/`, leave this one alone) — otherwise the controller's own execution ledger gets destroyed mid-run by the very work it's tracking. This workspace is still treated as scratch, never `git add`ed: it gets `rm -rf`'d at Finish per the skill's own process, same as any other SDD workspace. Cost if wrong: Task 3 wipes the ledger, and resuming after a compaction would have nothing to recover from except `git log` on the worktree branch.

Ruling: work happens in an isolated worktree (`.worktrees/chore-oss-readiness`, branch `chore/oss-readiness`) rather than directly on `main`, per subagent-driven-development's "never start on main without explicit consent" rule — the user picked "Subagent-Driven (recommended)" from the execution menu but was not separately asked about branch isolation. Task 1 and Task 7/10's branch-sync steps (creating `dev`/`test`, setting the GitHub default branch) are deferred to the Finish step, after this branch merges to `main` — creating `dev`/`test` from a mid-work `main` would be wrong, and the plan's own Task 7/10 already re-syncs those branches at multiple points, so deferring costs nothing. Cost if wrong: a few extra `git merge --ff-only` calls at Finish; no data at risk either way.

Pre-flight scan (writing-plans self-review, run before Task 1 dispatch):
- Task pairs sharing files: Task 2 (CONTRIBUTING.md, deletes AGENTS.md) and Task 4 (README.md Documentation table references CONTRIBUTING.md) — Task 4 depends on Task 2's file existing. Sequenced correctly (2 before 4) in the plan. No conflict.
- Task 3 (deletes docs/superpowers/plans, specs, .superpowers/sdd, PROSEGUI.md, README.md, docs/CONTINUE.md, docs/FIELD-TEST.md) runs after Task 2 (which already references "archived in Task 3" language — checked, language now says "deleted outright in Task 3", consistent) and before Task 4 (which removes the dead roadmap link Task 3's deletion creates). Sequenced correctly.
- Task 6 (models/README.md) depends on Task 3 having already removed the paths it used to cite — sequenced correctly (6 after 3).
- Task 8/9 (Phase B sweep) touch application source files Task 1-7 never touch (docs/READMEs only) — no file overlap, no conflict, can run after Phase A closes without re-checking Phase A's files.
- Self-consistency: Task 3 Step 1 moves the plan file itself out of the doomed directory before deleting it — verified this instruction is present and correctly ordered as Step 1 (before the `git rm -r` in Step 2).
- No placeholder patterns found in the plan text ("TBD", "similar to Task N", etc.) — full command blocks present in every step.
Scan is clean otherwise. Proceeding to Task 1.

Task 1: complete (controller-executed, no subagent — pure git-ref housekeeping with zero files to review). Deleted 14 stale local branches (fase-0..fase-6, fase-11, fase-11-geometry-pagination, two cursor/* branches), all previously confirmed merged into main earlier this session. `dev`/`test` creation and GitHub default-branch change deferred to Finish per the ruling above.

Task 2: complete (commits 1ec246d..adf08b5, review clean). Minor (deferred, spot-checked by controller and confirmed non-issue): reviewer flagged the implementer's "no AGENTS.md references in .github/workflows/" claim as unverified from the diff — controller ran the grep directly, confirmed empty, no fix needed.

Ruling: after Task 3 renamed the plan file itself (docs/superpowers/plans/2026-08-27-keeppix-oss-readiness.md -> ./OSS-READINESS-PLAN.md, as the plan's own Task 3 Step 1 requires), the sdd-workspace script's name-derived path changed too (.superpowers/sdd/2026-08-27-keeppix-oss-readiness/ -> .superpowers/sdd/OSS-READINESS-PLAN/). Consolidated all prior ledger/brief/report files into the new path so future script invocations (which now always resolve against OSS-READINESS-PLAN.md) stay consistent. No content lost — a plain move. Cost if wrong: none, purely a bookkeeping path change.

Task 3: complete (commits adf08b5..42a23e4, review clean). Reviewer independently verified the SDD-workspace exception at the filesystem/git-log level (confirmed respected — the one grep hit traced to commit-message prose, not a deleted file) and independently traced the implementer's extra-scope field-test-*.md / stray task-report.md deletions (confirmed genuinely in-category, no dangling references survive). Minor (deferred, cosmetic only): report prose miscounted "18" phase directories vs actual 17 — diff itself unaffected.

Task 4: complete (commits 42a23e4..91058ab, review clean). Minor (deferred): "Where it's going" sentence has a debatable subject-verb agreement ("a public website ... plus native desktop and mobile clients, are on the roadmap") — inherited verbatim from the plan's own prescribed text, not an implementer error. Left for the final whole-branch review to triage.

Task 5: complete (commits 91058ab..6e990cd, review clean).

Task 6: complete (commits 6e990cd..fa6f064, review pending — see next). Implementer flagged a real plan defect: scripts/download-yunet-sface.sh and .github/workflows/export-openclip-xlmr.yml still reference the deleted docs/superpowers/ path, and were never in Phase B's scope (which only lists crates/ and frontend/src/).

Ruling: the plan never accounted for scripts/*.sh, scripts/*.py, and .github/workflows/*.yml — controller-verified via grep: 7 files total have Italian comments (scripts/download-ai-bench.sh, scripts/download-yunet-sface.sh, scripts/field-test.sh, scripts/export-openclip-xlmr-it-en.py, .github/workflows/ci.yml, .github/workflows/export-openclip-xlmr.yml, .github/workflows/release.yml), 2 of which also reference the deleted docs/superpowers/ path. These are exactly the kind of file an external contributor reads (CI workflows, dev scripts) and belong in this initiative's scope. Adding as Task 6b, run now (small, low-risk, doc/comment-only) rather than deferred to Phase B's close, since it surfaced here. Cost if wrong: seven small files re-touched later instead of now — no real cost either way, just sequencing.

Task 6: complete (commits 6e990cd..fa6f064, review clean). Reviewer independently verified the benchmark numbers folded into prose against the deleted source doc (via git show on the pre-deletion commit) — confirmed accurate, not fabricated.

Task 6b: complete (commits fa6f064..0a98568, review clean). Reviewer independently verified the safety of the implementer's scope expansion (translating CI step name: labels and script output strings, not just # comments) by byte-diffing every id: field across base/head in both touched workflow files (identical) and checking every env:/with:/uses: value untouched — confirmed no behaviorally-significant string was altered. Minor (deferred, out of scope): a pre-existing untranslated "Task 22" comment in ci.yml line 95, not touched by this diff, noted for a future pass, not blocking.

Task 7: deferred to Finish in full (branch push/dev-test-sync/CI-on-main-check only make sense after this worktree branch actually merges into main — see the ruling at the top of this ledger). Nothing to execute at this point in the sequence. Proceeding directly to Phase B (Task 8).

Ruling: Task 8 (backend sweep) sub-batched further than the plan's literal "one crate per batch" — keeppix-db (74 files) and keeppix-api (82 files) are too large for one review pass to do well, so each splits into src/ vs tests/ (keeppix-api's src further splits into routes/ vs dav+misc). keeppix-dav has 0 Italian-comment files (grep-confirmed) — skipped entirely, no batch needed. keeppix-test-support (1 file) and keeppix-server (4 files) combined into one batch since both are trivially small. Final batch list for Task 8: (1) test-support+server, (2) domain, (3) media, (4) jobs, (5) db/src, (6) db/tests, (7) api/routes, (8) api/tests, (9) api/dav+misc. Cost if wrong: more dispatch overhead than strictly needed, but no correctness risk — finer granularity only improves reviewability.

Task 8 batch 1/9 (test-support+server): complete (commits 0a98568..a2f3c05..26e8a01, fix round 1/5: 1 addressed, 0 open). Review found dangling §9.4/§9.5/R9 citations to now-deleted internal docs (found by the reviewer independently tracing them through git history) — fixed by dropping the bare citation while keeping the substantive rationale, matching a pattern the implementer had already applied correctly elsewhere in the same diff. Re-review confirmed all three fixed, tests re-run and passing, no new breakage.

Task 8 batch 2/9 (keeppix-domain): complete (commits 26e8a01..2006f2a, review pending below). Implementer flagged two things: (1) crates/keeppix-domain/src/tag.rs has "Fase"/"Task N" phase-tracking comments not caught by the original accented-character grep (ASCII-only text, no accents) — a real detection-methodology gap; (2) crates/keeppix-domain/src/rename.rs has Italian-named string-literal placeholder tokens ({luogo}, {titolo}) used as actual rename-formula template syntax — this is a product/API surface decision (should user-facing rename placeholders be English?), not a comment-cleanup matter, correctly left untouched by the implementer, and out of scope for this plan.

Ruling: the original per-batch file lists (from Task 8/9's plan) were generated by grepping for accented Italian characters only, which misses ASCII-only "Fase N"/"Task N" comments. Controller-verified via broadened grep: 21 more backend files and 16 more frontend files carry this pattern with zero accents. Two of the 21 are in already-completed batches (keeppix-domain/src/tag.rs — batch 2, just done; keeppix-server/src/main.rs — batch 1, done). The rest fall into not-yet-dispatched batches (media, jobs, db, api) and will be absorbed into those batches' file lists directly. For Task 9 (frontend, not yet started), the 16 extra files will likewise be folded into that sweep's per-directory lists. The two retroactive files (tag.rs, main.rs) get one small combined fix dispatch now, run before batch 3. Cost if wrong: a handful of stray English-only phase-tracking comments survive somewhere in the tree — cosmetic, not a functional risk, and the final whole-branch review is instructed to spot-check for this pattern as a backstop.

Task 8 batch 2/9 (keeppix-domain): complete (commits 26e8a01..2006f2a, review clean, independently re-verified against live tree). Minor (deferred, cosmetic): awkward wording in face.rs, one doc-comment line-length inconsistency in token.rs.

Task 8 retroactive fix (tag.rs + main.rs): complete (commits 2006f2a..1b4aed6, review clean).

Task 8 batch 3/9 (keeppix-media): complete (commits 1b4aed6..d07074e, review clean, independently re-verified with comment-stripped-diff technique proving zero logic change). Implementer self-added tests/sandbox.rs (a real Italian assert string the verification grep caught) — legitimate, confirmed by reviewer.

Ruling: found a THIRD detection gap — plain unaccented Italian text (words like "questo", "nessuno", "assente", "fallito") evades both the accent grep and the Fase/Task grep entirely. Found via reviewer flagging crates/keeppix-media/src/{gpx,lib,video}.rs as still-Italian despite batch 3's "clean" verification. Built a broader Italian-function-word grep (nessun/questo/questa/quello/quella/senza/assente/illeggibile/fallito/durata/tramite/ovvero/cioè/perché/più/così/quindi/stesso/stessa/dell'/all'/un'/anche/molto/sempre/mai/ogni/proprio) — this is a heuristic, not exhaustive, but catches a meaningfully wider net. Fixed the 3 stragglers as retrofix2. Going forward (batches 4-9), file lists will be the union of all three detection methods (accented chars, Fase/Task pattern, broad word list) from the start, to avoid further retrofix rounds. Cost if wrong: some rare Italian phrasing that uses none of these listed words could still slip through — the final Task 10 whole-tree verification is the backstop, and will use this same broadened grep, not the original narrow one.

Task 8 retrofix (tag.rs+main.rs): complete (commits 2006f2a..1b4aed6, review clean).
Task 8 retrofix2 (media gpx.rs/lib.rs/video.rs): complete (commits d07074e..cda4ec5, review clean).

Task 8 batch 4/9 (keeppix-jobs): complete (commits cda4ec5..e2f6dd1, review clean). Reviewer directly ran `cargo test -p keeppix-jobs --test production_config` to confirm the DEPLOY.md-matching assertion string was correctly left untouched — test passes (the one unrelated failure in that binary is the pre-existing no-Docker sandbox limitation, not a regression).

Ruling: implementer flagged docs/DEPLOY.md (a top-level user-facing doc, referenced from README's Documentation table) still has Italian text, never in scope of any task in this plan (Phase A didn't touch it; Phase B only covers crates/ and frontend/src/). This is a real gap — DEPLOY.md is arguably more user-facing than internal code comments and should be English for a public release. Also: crates/keeppix-jobs/tests/production_config.rs string-matches DEPLOY.md's literal content, so translating DEPLOY.md must update that test's match string in the same commit or it breaks. Adding as Task 8c, to run after Task 8's crate sweep and before Task 9 (frontend), coordinating both files in one dispatch. Cost if wrong: DEPLOY.md stays Italian a bit longer — no functional risk, purely a completeness gap for the OSS-readiness goal.
