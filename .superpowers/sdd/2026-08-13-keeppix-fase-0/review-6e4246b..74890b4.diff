# Review package: 6e4246b..HEAD

## Commits
74890b4 fix(server): remove expect_used lint and fix healthcheck to honor configured bind

## Files changed
 crates/keeppix-server/src/config.rs |  2 +-
 crates/keeppix-server/src/main.rs   | 13 +++++--------
 2 files changed, 6 insertions(+), 9 deletions(-)

## Diff
diff --git a/crates/keeppix-server/src/config.rs b/crates/keeppix-server/src/config.rs
index 4632e94..a3718d6 100644
--- a/crates/keeppix-server/src/config.rs
+++ b/crates/keeppix-server/src/config.rs
@@ -32,21 +32,21 @@ struct Defaults {
     data_dir: PathBuf,
     db_max_connections: u32,
     session_ttl_secs: u64,
     log_format: LogFormat,
     allowed_origins: Vec<String>,
 }
 
 impl Default for Defaults {
     fn default() -> Self {
         Self {
-            bind: "0.0.0.0:5673".parse().expect("literal socket address"),
+            bind: SocketAddr::from(([0, 0, 0, 0], 5673)),
             data_dir: PathBuf::from("/data"),
             db_max_connections: 10,
             session_ttl_secs: 60 * 60 * 24 * 30,
             log_format: LogFormat::Json,
             allowed_origins: Vec::new(),
         }
     }
 }
 
 impl Config {
diff --git a/crates/keeppix-server/src/main.rs b/crates/keeppix-server/src/main.rs
index d2fac5c..28dcf04 100644
--- a/crates/keeppix-server/src/main.rs
+++ b/crates/keeppix-server/src/main.rs
@@ -1,11 +1,11 @@
-use std::path::PathBuf;
+use std::path::{Path, PathBuf};
 
 use anyhow::Context as _;
 use clap::{Parser, Subcommand};
 use keeppix_db::Db;
 use keeppix_server::config::Config;
 use keeppix_server::telemetry;
 
 #[derive(Parser)]
 #[command(name = "keeppix", version)]
 struct Cli {
@@ -26,21 +26,21 @@ enum Command {
     /// Verifica che il server locale risponda. Usato da HEALTHCHECK in Docker,
     /// dove non esistono né shell né curl.
     Healthcheck,
 }
 
 #[tokio::main]
 async fn main() -> anyhow::Result<()> {
     let cli = Cli::parse();
 
     if matches!(cli.command, Some(Command::Healthcheck)) {
-        return healthcheck().await;
+        return healthcheck(&cli.config).await;
     }
 
     let config = Config::load(Some(&cli.config))?;
     telemetry::init(config.log_format);
 
     let db = Db::connect(&config.database_url, config.db_max_connections)
         .await
         .context("connessione al database")?;
     db.migrate()
         .await
@@ -85,20 +85,17 @@ async fn shutdown_signal() {
     #[cfg(not(unix))]
     let terminate = std::future::pending::<()>();
 
     tokio::select! {
         () = ctrl_c => {},
         () = terminate => {},
     }
     tracing::info!("shutting down");
 }
 
-async fn healthcheck() -> anyhow::Result<()> {
-    let port = std::env::var("KEEPPIX_BIND")
-        .ok()
-        .and_then(|b| b.rsplit(':').next().and_then(|p| p.parse::<u16>().ok()))
-        .unwrap_or(5673);
+async fn healthcheck(config_path: &Path) -> anyhow::Result<()> {
+    let config = Config::load(Some(config_path))?;
 
-    let stream = tokio::net::TcpStream::connect(("127.0.0.1", port)).await?;
+    let stream = tokio::net::TcpStream::connect(("127.0.0.1", config.bind.port())).await?;
     drop(stream);
     Ok(())
 }
