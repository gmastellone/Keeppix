[package]
name = "keeppix-test-support"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
# Not production code: it exists only as a `[dev-dependencies]` entry for
# other crates, which have no other way to share test code.
publish = false

[dependencies]
http = "1"

[lints]
workspace = true
