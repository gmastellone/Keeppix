# Functions and routes that exist without a production consumer.
# Format: fn|route <name> <phase>
#
# Two sections, declared:
#   Rinvii — the consumer lives in a phase that has not run yet.
#            <phase> is that future phase (or ops/ci).
#   Debiti — shipped in a phase that already closed, with no consumer.
#            <phase> is the phase that will pay the debt, NOT the origin.
#            Origin is in the comment on the line.
#
# A name with no consumer and no entry here is a defect. Do not add a
# name to silence the guard without putting it in the right section.
#
# Do not list TrashRepo::cleanup_expired or /ws|/ws/ticket: those were
# the Fase 2R3 defects this guard exists to catch (Tasks 4 and 6).

# =============================================================================
# Rinvii — consumatore in una fase non ancora eseguita
# =============================================================================
route /health ops
route /api/openapi.json ci

# move_asset, render_filename, resolve_place_label: erano qui in attesa dei
# consumatori di Fase 9 (Task 4/8 e 7/8). Fase 9 è chiusa e mergiata —
# verificato il 26 agosto 2026 rimuovendo temporaneamente queste tre righe e
# rilanciando `check-wired.py`: passa lo stesso, e `grep` conferma call-site
# reali fuori dai test (`move_asset` in `culling.rs`/`rename.rs`/`assets.rs`;
# `render_filename`/`resolve_place_label` nel motore di rinomina di
# `rename.rs`). Rimosse, non più eccezioni.

# =============================================================================
# Debiti — spediti in una fase già chiusa senza consumatore
# =============================================================================
# origine fase-0 (sessioni). /auth/refresh: pagato in 12b (watchdog SPA).
# ping (keeppix-db/src/lib.rs): pagato il 26 agosto — GET /health (router con
# stato) ora chiama `Db::ping` per davvero e riporta `database: "ok"` o
# `"unreachable"` nel corpo della risposta (routes/health.rs). La variante
# senza stato (router_without_state, usata dai test che non vogliono
# Postgres) resta senza database, onestamente `"not checked"`, non `"ok"`
# finto.

# origine fase-1a. get_json: pagato — `transcode.rs` rilegge `capabilities`
# (Fase 6) e Fase 7 Task 1 estende quel JSON con `extra.ai`. Non è più un rinvio.
# ensure_child (folders.rs): era qui in attesa che Fase 9 lo riusasse per le
# sottocartelle _taken/_skipped del culling — non è successo, Fase 9 ha
# scritto una funzione privata separata (`ensure_culling_child_on`, marca
# anche `culling_role`) invece di riusare questa. Ma `ensure_child` ha un
# consumatore reale indipendente e pre-esistente, mai notato quando questa
# voce fu scritta: `crates/keeppix-api/src/dav/write.rs` (MKCOL via WebDAV,
# da Fase 5, commit `4d2564e`, 19 agosto). Verificato il 26 agosto rimuovendo
# la riga e rilanciando `check-wired.py`: passa. Rimossa, non più eccezione.

# origine fase-1b. count_by_status (keeppix-db/src/assets.rs): conta gli
# asset per stato di scansione, scoped alla visibilità del chiamante. La
# vecchia voce qui diceva che l'evento `scan.progress` (Fase 10) la
# espone in WS — falso, verificato leggendo `drain_scan_progress`
# (crates/keeppix-api/src/routes/ws.rs): quell'evento usa
# `AssetRepo::count_in_library`, una funzione diversa, con un commento
# che lo dichiara di proposito ("stessa fonte già usata da GET
# /libraries/{id}/scan... non un secondo stato inventato"). `count_by_status`
# è un secondo percorso rimasto scollegato fin dall'origine, non lo stesso
# codice con un nome diverso — stessa forma di `get_for_user` più sotto.
# Nessuna schermata di Fase 11 mostra un conteggio per-stato ("N indexed,
# M pending, K failed"): verificato su `ProblemsView.vue` e sulle altre
# viste che elencano librerie. Non riassegnata a una fase specifica di
# proposito — va decisa (wired per davvero, o rimossa se ridondante) la
# prossima volta che si tocca lo stato di scansione, non lasciata scorrere
# in silenzio.
fn count_by_status non-rivendicata

# origine fase-10 Task 16 (non Fase 11: il Task 16 della Fase 11 è
# "Persone", tutt'altra cosa). `POST /operations/{id}/cancel` e l'intero
# canale WS `operation.progress` (avanzamento dei job lunghi — rinomina in
# blocco, embedding, rilevamento volti) sono pronti dal backend dalla Fase
# 10, ma nessun Task della Fase 11 ha mai davvero costruito il pannello
# "operazioni in corso" che li consumerebbe: verificato — zero riferimenti
# a "operation.progress"/"cancelOperation" in tutto `frontend/src`, nessun
# wrapper API per questa rotta, nessun Task del piano (docs/superpowers/
# plans/2026-08-20-keeppix-fase-11.md) la rivendica esplicitamente. Non un
# rinvio piccolo: manca un'intera superficie UI (avanzamento + annulla),
# non una chiamata isolata. Non riassegnata a una fase specifica di
# proposito — va decisa la prossima volta che si tocca quest'area.
route /operations/{id}/cancel non-rivendicata

# origine fase-4 (mappe/geotag). get_for_user: legge la posizione "casa" di un
# utente (si scrive con /users/me/home ma non si rilegge da nessuna route).
# timezone_for / timezone_changes / apply_taken_at_batch: verificate il 20
# agosto 2026 — apply_timezone_changes (nome diverso, geotag.rs) è la funzione
# davvero wired dietro /metadata/batch/recalculate-timezones; queste tre sono
# un secondo percorso rimasto scollegato, non lo stesso codice con un nome
# diverso. Nessuna fase del roadmap le rivendica: l'amministrazione del server
# è dichiarata fuori dal disegno dell'interfaccia (Parte XII del documento
# funzionale). Non riassegnate a una fase specifica di proposito — vanno
# decise (wired per davvero, o rimosse se ridondanti) la prossima volta che
# si tocca home/timezone, non lasciate scorrere in silenzio.
fn get_for_user non-rivendicata
fn timezone_for non-rivendicata
fn timezone_changes non-rivendicata
fn apply_taken_at_batch non-rivendicata
