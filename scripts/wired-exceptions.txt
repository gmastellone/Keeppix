# Functions and routes that exist without a production consumer.
# Format: fn|route <name> <phase>
#
# Two sections, declared:
#   Rinvii — the consumer lives in a phase that has not run yet.
#            <phase> is that future phase (or ops/ci).
#   Debiti — shipped in a phase that already closed, with no consumer.
#            <phase> is the phase that will pay the debt, NOT the origin.
#            Origin is in the comment on the line.
#
# A name with no consumer and no entry here is a defect. Do not add a
# name to silence the guard without putting it in the right section.
#
# Do not list TrashRepo::cleanup_expired or /ws|/ws/ticket: those were
# the Fase 2R3 defects this guard exists to catch (Tasks 4 and 6).

# =============================================================================
# Rinvii — consumatore in una fase non ancora eseguita
# =============================================================================
route /health ops
route /api/openapi.json ci

# move_asset, render_filename, resolve_place_label: erano qui in attesa dei
# consumatori di Fase 9 (Task 4/8 e 7/8). Fase 9 è chiusa e mergiata —
# verificato il 26 agosto 2026 rimuovendo temporaneamente queste tre righe e
# rilanciando `check-wired.py`: passa lo stesso, e `grep` conferma call-site
# reali fuori dai test (`move_asset` in `culling.rs`/`rename.rs`/`assets.rs`;
# `render_filename`/`resolve_place_label` nel motore di rinomina di
# `rename.rs`). Rimosse, non più eccezioni.

# =============================================================================
# Debiti — spediti in una fase già chiusa senza consumatore
# =============================================================================
# origine fase-0 (sessioni). /auth/refresh: pagato in 12b (watchdog SPA).
# ping (keeppix-db/src/lib.rs): pagato il 26 agosto — GET /health (router con
# stato) ora chiama `Db::ping` per davvero e riporta `database: "ok"` o
# `"unreachable"` nel corpo della risposta (routes/health.rs). La variante
# senza stato (router_without_state, usata dai test che non vogliono
# Postgres) resta senza database, onestamente `"not checked"`, non `"ok"`
# finto.

# origine fase-1a. get_json: pagato — `transcode.rs` rilegge `capabilities`
# (Fase 6) e Fase 7 Task 1 estende quel JSON con `extra.ai`. Non è più un rinvio.
# ensure_child (folders.rs): era qui in attesa che Fase 9 lo riusasse per le
# sottocartelle _taken/_skipped del culling — non è successo, Fase 9 ha
# scritto una funzione privata separata (`ensure_culling_child_on`, marca
# anche `culling_role`) invece di riusare questa. Ma `ensure_child` ha un
# consumatore reale indipendente e pre-esistente, mai notato quando questa
# voce fu scritta: `crates/keeppix-api/src/dav/write.rs` (MKCOL via WebDAV,
# da Fase 5, commit `4d2564e`, 19 agosto). Verificato il 26 agosto rimuovendo
# la riga e rilanciando `check-wired.py`: passa. Rimossa, non più eccezione.

# origine fase-1b. count_by_status (keeppix-db/src/assets.rs): conta gli
# asset per stato di scansione, scoped alla visibilità del chiamante. La
# vecchia voce qui diceva che l'evento `scan.progress` (Fase 10) la
# espone in WS — falso, verificato leggendo `drain_scan_progress`
# (crates/keeppix-api/src/routes/ws.rs): quell'evento usa
# `AssetRepo::count_in_library`, una funzione diversa, con un commento
# che lo dichiara di proposito ("stessa fonte già usata da GET
# /libraries/{id}/scan... non un secondo stato inventato"). `count_by_status`
# è un secondo percorso rimasto scollegato fin dall'origine, non lo stesso
# codice con un nome diverso — stessa forma di `get_for_user` più sotto.
#
# Decisione presa il 26 agosto, non più rinviata: nessuna schermata del
# documento funzionale chiede un conteggio per-stato ("N indexed, M
# pending, K failed") — verificato di nuovo, sia su `ProblemsView.vue`
# (riscritta riga-per-riga contro §47, non lo prevede) sia sull'intero
# `docs/ui/documento-funzionale-ui.md` (nessuna occorrenza). Costruire una
# UI apposta significherebbe inventare una schermata che il disegno non ha
# mai chiesto. **Non è però codice morto**: `count_by_status` è la sonda
# usata da sei asserzioni reali in `tests/albums.rs`, `tests/assets.rs` e
# `tests/permissions.rs` per verificare `VisibilityScope` (chi vede cosa
# multi-tenant) da più angolazioni — toglierla indebolirebbe quella
# copertura senza un sostituto equivalente. Resta come API interna
# testata, senza rotta HTTP: non un debito, un'eccezione permanente.
fn count_by_status permanente

# origine fase-10 Task 16. Pagato in parte il 26 agosto — non tutto,
# onestamente: erano tre feature diverse dietro un'unica voce, non un
# rinvio piccolo, e la loro forma tecnica non è la stessa.
#
# LibraryScan: chiuso per davvero. `POST /libraries/{id}/scan` risponde
# `202` con `operation_id` **subito** (job reale in background) — in
# `ProblemsView.vue`, un riavvio di connessione riuscito (`retryConnection`)
# ora avvia una scansione vera e ne segue `operation.progress` sul
# WebSocket con una barra reale e un `[Annulla]` che chiama davvero questa
# rotta (`api/operations.ts::cancelOperation`). Non più un'eccezione.
#
# BulkRename e AiAnalysis/FaceDetection: NON costruiti, di proposito —
# non sono la stessa forma di LibraryScan.
# - `BulkRename` (`POST /assets/batch/rename`, `RenameFormulaDialog.vue`):
#   la rinomina intera gira dentro la richiesta HTTP
#   (`RenameRepo::apply`, sincrona) e `operation_id` torna solo a batch
#   già finito — un annullamento reale non è realizzabile lato client
#   senza cambiare la forma della risposta (farla tornare subito e far
#   girare il batch altrove), che non è stato fatto qui.
# - `AiAnalysis`/`FaceDetection`: nessuna rotta le innesca (finestre
#   automatiche in background, verificato — zero `OperationKind::AiAnalysis`/
#   `FaceDetection` fuori da `keeppix-jobs`), quindi non c'è un punto
#   d'ingresso utente a cui agganciare un dialog di progresso: servirebbe
#   un indicatore ambiente separato, non costruito qui.
# Nessuna fase le rivendica esplicitamente — restano decise così finché
# qualcuno non decide di investire nella forma diversa che richiedono.

# origine fase-4 (mappe/geotag). Decisione presa il 26 agosto, non più
# rinviata — le quattro erano raggruppate insieme ma non sono lo stesso
# caso:
#
# get_for_user (home.rs) e timezone_changes (geo.rs) erano duplicati morti
# per davvero — zero chiamate ovunque, nemmeno nei test (verificato con
# grep sull'intero albero `crates/`). `get_for_user` duplicava
# `HomeRepo::get` (che legge la casa dell'utente *autenticato*; questa
# prendeva un `UserId` arbitrario, mai usata da alcuna rotta admin).
# `timezone_changes` duplicava `timezone_changes_on`, la stessa
# `timezone_changes_sql()` dietro un wrapper pubblico mai chiamato — il
# vero percorso di anteprima passa da `GeoRepo::timezone_change_preview`,
# un terzo nome ancora. Rimosse entrambe, non più eccezioni.
#
# timezone_for e apply_taken_at_batch invece NON sono morte: stesso caso
# di `count_by_status` sopra. `timezone_for` e la vera
# `timezone_candidates_sql()` (dietro `apply_timezone_changes`, wired)
# chiamano entrambe `timezone_match_sql()` — lo stesso predicato spaziale
# `ST_Covers`. I test di `timezone_for` in `tests/geo.rs` (Tokyo trovato,
# oceano non trovato, punto su un confine condiviso senza doppio match né
# errore) sono l'unica copertura reale di quel predicato — `tests/
# timezones.rs` (il percorso vero) verifica solo permessi/autenticazione,
# mai la geometria del confine. `apply_taken_at_batch` e la vera
# `apply_timezone_changes` chiamano entrambe
# `apply_taken_at_assignments_in_transaction`; il test di
# `apply_taken_at_batch` in `tests/overrides.rs`
# (`timezone_writer_preserves_a_taken_at_override_present_at_write_time`)
# è l'unica copertura della regola «un override manuale precedente non
# va sovrascritto dalla correzione fuso orario in blocco» — non replicata
# altrove. Cancellarle perderebbe copertura reale su codice di produzione
# senza un sostituto equivalente: restano API interne testate, senza
# rotta HTTP propria, eccezioni permanenti.
fn timezone_for permanente
fn apply_taken_at_batch permanente
